# Pneumonia Detection using Vision Transformers (ViT)

## 📖 Описание проекта

Этот проект реализует пайплайн для автоматического детектирования пневмонии на рентгеновских снимках грудной клетки (Chest X-Ray) с использованием архитектуры Vision Transformer (ViT). Мы используем модель google/vit-base-patch16-224, дообученную на медицинских данных.

Проект разработан с учетом MLOps практик:

- Управление зависимостями: Poetry.
- Конфигурация: Hydra (гибкое управление параметрами через .yaml и CLI).
- Версионирование данных: DVC.
- Качество кода: Автоматические проверки через Pre-commit (Black, Isort, Flake8).
- Воспроизводимость: Фиксация random seeds и версий библиотек.

## 📊 Датасет

Используется датасет Chest X-Ray Images (Pneumonia).

- Классы: NORMAL (Здоров), PNEUMONIA (Пневмония).
- Формат: JPEG изображения разных размеров (приводятся к 224x224).

## 🛠 Setup (Настройка окружения)

Следуйте этой инструкции для развертывания проекта на новой машине.

Предварительные требования

- Python 3.9+
- Установленный Poetry
- DVC

### Установка

Клонируйте репозиторий:

```
git clone https://github.com/SofiMi/mlOps.git
cd mlOps
```

Установите зависимости:
Команда создаст виртуальное окружение и установит все необходимые библиотеки (включая dev-инструменты).

```
poetry install
```

Настройте Pre-commit хуки:
Это обязательно для проверки качества кода перед коммитом.

```
poetry run pre-commit install
```

Проверка: Запустите хуки на всех файлах, чтобы убедиться в корректности установки.

```
poetry run pre-commit run -a
```

Загрузите данные (DVC):
Если у вас настроен удаленный репозиторий (S3/Drive), выполните:

```
poetry run dvc pull
```

Или положите папку с данными (chest_xray) вручную в data/raw/.

## 🚀 Train

Пайплайн обучения включает загрузку данных, оверсемплинг (балансировка классов), аугментацию и дообучение модели ViT. Все параметры управляются через Hydra.

Запуск обучения с параметрами по умолчанию:

```
poetry run python src/ml_project/train.py
```

Изменение параметров через CLI:
Вы можете менять количество эпох, batch size или learning rate без изменения кода:

```
poetry run python src/ml_project/train.py trainer.num_train_epochs=5 model.learning_rate=1e-5
```

Процесс обучения автоматически сохранит лучшую модель в папку models/vit-pneumonia.

## 🏭 Production Preparation

Для оптимизации инференса и запуска на прод-окружении (где может не быть тяжелых библиотек), мы конвертируем модель в формат ONNX.

Экспорт модели в ONNX:

```
poetry run python src/ml_project/to_onnx.py
```

Результат: Файл будет сохранен по пути models/model.onnx.

Артефакты поставки:
В продакшен передаются:

models/model.onnx — оптимизированная модель.
configs/ — файлы конфигурации (например, маппинг классов).
src/ml_project/infer.py — скрипт инференса (использует onnxruntime).

## 🔮 Infer (Инференс)

Запуск модели для предсказания на новых данных. Инференс реализован отдельно и может работать с ONNX-моделью для максимальной скорости.

Команда:

```
poetry run python src/ml_project/infer.py image_path="путь/к/изображению.jpeg"
```

Пример:

```
poetry run python src/ml_project/infer.py image_path="data/raw/chest_xray/val/PNEUMONIA/person1946_bacteria_4874.jpeg"
```

Формат вывода:
Скрипт выведет JSON с предсказанным классом и уверенностью модели:

```
{
  "filename": "person1946_bacteria_4874.jpeg",
  "prediction": "PNEUMONIA",
  "confidence": 0.985
}
```

### Структура проекта

```
├── configs/              # Конфигурационные файлы Hydra (YAML)
├── data/                 # Данные (raw / processed) - под управлением DVC
├── models/               # Сохраненные модели и артефакты
├── src/
│   └── ml_project/       # Исходный код пакета
│       ├── __init__.py
│       ├── train.py      # Скрипт обучения
│       ├── infer.py      # Скрипт предсказания
│       ├── to_onnx.py    # Скрипт конвертации в ONNX
│       └── ...
├── .pre-commit-config.yaml
├── pyproject.toml        # Управление зависимостями
└── README.md
```
